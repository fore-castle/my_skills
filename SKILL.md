---
name: my-memory
description: >-
  指导 agent 判断"什么值得记、怎么记、何时淘汰"的记忆管理技能。
  触发场景：用户纠正 agent 行为、陈述偏好/规则、实验得出结论、
  踩到工具/环境坑、项目交接/总结、验证历史结论、重复提醒检测。
  核心能力：三层过滤判断捕获、结论溯源(证据链+置信度)、
  项目生命周期记忆(决策日志/踩坑记录/探索日志)、蒸馏与淘汰。
  覆盖科研实验记录和项目编码偏好两个核心领域。
  跨平台兼容 QoderWork / Cursor / Claude Code。
version: 1.1.0
---

# My Memory — 让 agent 知道该记什么

## 核心原则

记忆的目标是**高信息密度、低检索噪声**。
每一条记忆必须回答：这条信息能让未来的 agent 少犯什么错？
如果答不上来，就不值得记。

---

## 1. 捕获判断：什么值得记

### 1.1 信号识别

当对话中出现以下信号时，评估是否写入记忆：

| 信号 | 示例 | 捕获倾向 |
|------|------|----------|
| 用户纠正 agent | "不对，应该用 X 而不是 Y" | **必记** — 这是偏好/规则 |
| 用户显式声明规则 | "以后不要做 X"、"记住 Y" | **必记** |
| 实验得出反直觉结论 | "A 比 B 好，因为..." | **必记** — 反直觉才值得记 |
| 踩到非显而易见的坑 | "这个 API 的返回格式其实是..." | **必记** — 避免重复踩坑 |
| 实验得出预期结论 | "A 比 B 好 2pp" | **条件记** — 仅当结论影响后续决策 |
| 用户陈述工作习惯 | "我一般先看 X 再看 Y" | **条件记** — 仅当影响 agent 行为 |
| 一次性事实/数据 | "这次跑了 5000 条样本" | **不记** — 写进日志即可 |
| 可从代码/配置推断的信息 | "config.yaml 里 lr=0.001" | **不记** — 源文件就是真相 |

### 1.2 三层过滤器

通过信号识别后，再过三层过滤：

**F1 — 可重现性**：这条信息未来还会用到吗？
- 会 → 通过
- 仅当前会话需要 → 不记，留在上下文里

**F2 — 信息增量**：现有记忆里是否已有等价或更新的版本？
- 没有 / 这是补充 → 通过
- 已有 → 考虑替换旧条目（见 §4 冲突解决）

**F3 — 可推导性**：能否从代码/配置文件/文档直接推导出来？
- 不能 / 推导成本很高 → 通过
- 能 → 不记，改为记录"去哪里找"（路径/文件名）

三层全通过 → 写入记忆。任一不通过 → 丢弃或仅写入当日日志。

### 1.3 速判流程图

```
信号出现？
  ├─ 用户纠正/显式规则/反直觉结论/非显然坑 → 直接写入
  ├─ 其他信号 → F1 未来还会用到？
  │    ├─ 否 → 丢弃
  │    └─ 是 → F2 记忆里已有？
  │         ├─ 已有且更新 → 替换旧条目
  │         └─ 没有/补充 → F3 能从代码推导？
  │              ├─ 能 → 记"去哪找"而非内容
  │              └─ 不能 → 写入
```

### 1.4 重复提醒检测（防遗忘闭环）

用户纠正 agent 时，agent 必须执行以下检查：

1. **写入前检索**：先用 `memory_search` 搜索是否已有相关记忆
2. **如果已存在** → 说明之前写了但没生效（检索失败或上下文未加载）
   - 检查该条目的位置和格式是否可被正确检索到
   - 提升格式：加粗关键词、调整标题使其更易命中
   - 向用户确认："这条我之前记录过，但显然没起到作用。我来调整它的格式/位置让下次更容易命中。"
3. **如果不存在** → 正常写入，标记为 P0 或 P1
4. **如果同一件事被用户提醒 ≥ 2 次** → 自动升级为 P0（不可违反的规则），并在条目中追加：
   ```
   ⚠️ 用户已提醒 N 次。必须严格遵守。
   ```

**关键**：不要只在用户说"记住这个"时才检索。每次用户纠正行为（"不对"、"不要这样"、"我说过 X"）都触发检索。

---

## 2. 优先级分类

### P0 — 不可违反的规则
用户明确设定的硬性约束。违反会导致严重后果。
标记：`importance: critical`
示例：
- "终止 Primus 任务前必须先问用户确认"
- "修改 pipeline 配置必须新建 yaml，不得改旧配置"

### P1 — 反复适用的约定
跨会话持续有效的工作约定、编码规范、工具链坑点。
标记：`importance: normal`
示例：
- "matplotlib 中文字体用 Arial Unicode MS，无 PingFang"
- "统计占比必须先跑代码，不许凭假设推测"

### P2 — 阶段性的结论
当前项目阶段的实验结论、决策记录。可能随项目演进而过期。
标记：`importance: normal`，附带 `[有效期至: YYYY-MM-DD]` 或 `[待验证]`
示例：
- "v3p2 覆盖度分析结论：缩写占失败 50.6%，是最大失败来源"
- "GKD 短序列用 lmbda=0 的 teacher-forcing 比 on-policy 安全"

### 不记 — 日志级信息
一次性数据、中间结果、可从源文件推导的配置。
写入 `.my_memory/daily/YYYY-MM-DD.md` 即可，不进长期记忆。

---

## 3. 写入格式

### 3.1 条目结构

每条记忆必须包含：

```markdown
## [简明标题 — 能检索到的关键词]
- [核心事实 1 — 一行说完]
- [核心事实 2]
- [如有数字，带上关键数字，但不要堆砌]

**重要性**: critical / normal
[可选] **有效期**: YYYY-MM-DD / 待验证 / 长期有效
[可选] **来源**: 哪次实验/哪个文件/用户原话
§
```

规则：
- 标题必须包含可检索的关键词，不要写"其他注意事项"这种模糊标题
- 每个 bullet 独立可读，不依赖上下文
- 数字只记关键结论数字，不记中间计算过程
- 用 `§` 标记条目结束（兼容现有 memory 解析）

### 3.1.1 结论溯源格式（P2 条目必填）

P2 级实验/分析结论必须附带溯源信息，方便后续验证。格式：

```markdown
## [结论标题]
- **结论**: [一句话结论]
- **置信度**: 高(数据验证) / 中(推理得出,未验证) / 低(假设,待验证)
- **证据链**:
  - 数据来源: [哪个文件/哪次实验/哪个命令的输出]
  - 关键步骤: [推理或计算的简要过程,2-3步]
  - 前提假设: [依赖了什么假设/条件]
- **验证状态**: 已验证 / 待验证 / 已被推翻
- **日期**: YYYY-MM-DD
§
```

规则：
- **结论和证据必须分离**：结论可以压缩进摘要，但证据链必须保留在日志或独立文件中，用路径引用
- **置信度必填**：不允许出现无置信度的结论。"高"必须有具体数据支撑；"中"要标注验证方法；"低"要标注验证条件
- **推理过程记在日志里**：`.my_memory/research/` 中只放证据链摘要 + 日志路径，完整推导过程写入 `.my_memory/daily/YYYY-MM-DD.md`
- **被推翻的结论不删除**：改为标注 `**验证状态**: 已被推翻(YYYY-MM-DD)，正确结论见 [链接]`，保留推理过程供复盘

示例（好的 vs 差的）：

```markdown
# 差的 — 无溯源，无法验证
## 覆盖度分析
- 缩写占失败 50.6%，是最大失败来源
§

# 好的 — 有溯源，可验证
## 覆盖度分析 — 缩写失败占比
- **结论**: 缩写输入占全部失败的 50.6%，是最主要的失败来源
- **置信度**: 高（数据验证）
- **证据链**:
  - 数据来源: `analyze_hard_input_coverage.py` 输出, 样本集 `miss_hard_input.jsonl`(1409条)
  - 关键步骤: 按 `_eval_meta.mode` 分组统计 → abbreviation 组 714/1409=50.6%
  - 前提假设: mode 标签准确；包含口径（对齐窗口）
- **验证状态**: 已验证
- **日期**: 2025-03-15
- 详细过程: memory/2025-03-15.md#覆盖度分析
§
```

### 3.2 文件组织

按 §6 的统一存储结构分文件写入：偏好 → `personal.md`，约定 → `conventions.md`，科研结论 → `research/[topic].md`，决策 → `decisions.md`。

### 3.3 容量预算

| 区域 | 预算 | 超限处理 |
|------|------|----------|
| 用户偏好与硬性规则 | 不限（通常很短） | — |
| 编码约定与工具链 | ~2000 bytes | 合并同类、淘汰过期 |
| 单个科研子方向 | ~2500 bytes | 蒸馏：旧结论压缩为一句 + 指向日志 |
| 决策日志 | 最近 10 条 | 更早的移入日志或归档 |
| `.my_memory/` 总计 | < 8000 bytes | 触发蒸馏周期（§5） |

---

## 4. 冲突解决

当新信息与已有记忆矛盾时：

1. **P0 规则冲突**：用户最新指示 > 一切。立即更新，标注变更日期。
2. **P1 约定冲突**：新版本替换旧版本。旧版本不保留，除非有明确"两种都可用"的语境。
3. **P2 结论冲突**：
   - 如果是同一实验的更新数据 → 替换
   - 如果是不同条件下的不同结论 → 并列保留，显式标注各自适用条件
   - 标注 `[待用户确认]` 如果不确定哪个是对的

**绝对禁止**：新旧矛盾结论共存而不标注。如果发现这种情况，主动向用户确认哪个是当前有效的。

---

## 5. 蒸馏与淘汰

### 5.1 触发条件

当 `.my_memory/` 总计超过 80% 容量，或每 10 次会话（以先到者为准），执行蒸馏。

### 5.2 蒸馏流程

1. **扫描 P2 条目**：检查所有带 `[有效期]` 或 `[待验证]` 的条目
   - 已过期 → 压缩为一句结论 + "详见 .my_memory/daily/YYYY-MM-DD.md"
   - 已验证为正确 → 升级为 P1（去掉有效期标记）
   - 已验证为错误 → 删除，或替换为正确结论

2. **合并同类**：同一主题下多个零散 bullet → 合并为一个紧凑段落
   - 保留结论性数字，删除过程性描述
   - 如果合并后仍太长 → 详细内容移入 `memory/topic-deep-dive.md`，长期记忆只留摘要 + 路径

3. **淘汰候选**：
   - 超过 30 天未被任何会话引用的 P2 条目 → 降级为日志引用
   - 可从当前代码/配置直接推导的 P1 条目 → 替换为"见 [文件路径]"
   - 用户已不再活跃的项目相关条目 → 整体归档

### 5.3 蒸馏输出

蒸馏后向用户报告：
```
记忆蒸馏完成：
- 压缩 X 条 → Y 条
- 淘汰 Z 条过期/可推导条目
- 当前容量：N/10240 bytes (M%)
```

---

## 6. 跨平台适配

`.my_memory/` 是唯一的记忆存储地。各平台不维护独立的记忆文件，而是通过桥接机制读取 `.my_memory/` 的内容。

### 统一存储结构

```
[工作目录]/
├── .my_memory/
│   ├── personal.md            # 个人偏好与硬性规则（P0 + P1）
│   ├── conventions.md         # 编码约定与工具链坑点
│   ├── research/              # 科研结论，按项目/子方向分文件
│   │   ├── ime-data-pipeline.md
│   │   ├── ime-model-train.md
│   │   └── mot-tracking.md
│   ├── decisions.md           # 跨项目重大决策
│   ├── projects/              # 项目级记忆，按项目分目录
│   │   ├── [project-a]/
│   │   │   ├── overview.md
│   │   │   ├── decisions.md
│   │   │   ├── pitfalls.md
│   │   │   └── exploration-log.md
│   │   └── [project-b]/
│   │       └── ...
│   └── daily/                 # 日志，按日期
│       └── YYYY-MM-DD.md
```

### 各平台桥接方式

**QoderWork**：
- 启动时读取 `.my_memory/personal.md` → 用 `memory` 工具 add/replace 同步到 MEMORY.md
- 会话中的记忆写入仍走 `memory` 工具，定期批量同步回 `.my_memory/`
- 项目记忆直接读写 `.my_memory/projects/[name]/` 下的文件

**Cursor**：
- 在 `.cursor/rules/` 中创建一个 `my-memory.mdc`，内容为一行指令：`读取 .my_memory/personal.md 和 .my_memory/conventions.md 作为工作约定`
- 项目记忆直接读写 `.my_memory/projects/[name]/`

**Claude Code**：
- 在 `CLAUDE.md` 中加一行：`工作约定和项目记忆存放在 .my_memory/ 目录下，每次会话开始时读取 personal.md 和 conventions.md`
- 项目记忆直接读写 `.my_memory/projects/[name]/`

### 桥接原则
- **写入**：始终写入 `.my_memory/`，不写平台原生记忆文件（QoderWork 的 memory 工具除外，它作为缓存层）
- **读取**：各平台启动时桥接读取 `.my_memory/` 中的对应文件
- **迁移**：换平台时只需配置桥接指令，记忆数据无需搬迁

---

## 7. 日志写入规范

日志（`.my_memory/daily/YYYY-MM-DD.md`）是宽松区域，但仍需结构化：

```markdown
## YYYY-MM-DD

<!-- memory-chat:{chatId} -->
## Session: [一句话标题]
- [做了什么]
- [关键发现/结论]
- [待办/下一步]
<!-- /memory-chat:{chatId} -->
```

日志中可以包含：
- 实验的完整数字和中间结果
- 临时的调试过程
- 待确认的假设
- 下次会话的 TODO

日志中不需要：
- 已在 `.my_memory/personal.md` 或 `conventions.md` 中的重复内容（引用即可）
- 纯寒暄/闲聊内容

---

## 8. 项目生命周期记忆

### 8.1 问题

个人记忆（`.my_memory/personal.md`）适合存偏好和约定，但不适合存项目探索过程。
一个科研项目可能持续数月，经历多个方向尝试、方案否定、参数调优。
如果只在会话结束时做总结，早期的探索、被否定的方案、踩过的坑都会丢失。

### 8.2 方案：项目级记忆文件

每个活跃项目在 `.my_memory/projects/[project-name]/` 下维护项目记忆：

```
.my_memory/projects/[project-name]/
├── overview.md          # 项目概况（短，<50行）
├── decisions.md         # 决策日志（按时间倒序）
├── pitfalls.md          # 踩坑记录
└── exploration-log.md   # 探索日志（持续追加）
```

**位置说明**：
- 所有项目记忆统一存放在 `.my_memory/projects/` 下
- 如果项目的 git 仓库在工作目录内，也可在仓库根目录创建软链接指向 `.my_memory/projects/[name]/`

### 8.3 各文件职责与格式

**overview.md** — 项目概况，新会话的第一个读取文件

```markdown
# [项目名] 概况
- **目标**: [一句话]
- **当前阶段**: [探索/开发/调优/收尾]
- **关键路径**: [核心代码/数据/配置的位置]
- **当前结论**: [最重要的 2-3 个结论，带日期]
- **未解决问题**: [当前阻塞项]
- **最近更新**: YYYY-MM-DD
```

容量：< 50 行。超限时压缩旧结论为引用。

**decisions.md** — 决策日志，记录"为什么这样做"

```markdown
### YYYY-MM-DD [决策标题]
- **决策**: 选择了方案 A
- **理由**: [为什么选 A，关键数据/推理]
- **被否方案**:
  - 方案 B: [否定原因]
  - 方案 C: [否定原因]
- **前提假设**: [这个决策依赖什么假设，如果假设不成立需要重新评估]
- **状态**: 有效 / 已推翻(YYYY-MM-DD, 原因: ...)
```

规则：
- 每个决策必须记录**被否方案及否定原因**——这是交接的核心价值
- 决策被推翻时不删除，标注状态并追加新决策
- 定期清理已推翻且无参考价值的旧决策

**pitfalls.md** — 踩坑记录，防止重复踩坑

```markdown
### YYYY-MM-DD [坑的简述]
- **现象**: [看到了什么]
- **根因**: [实际原因]
- **排查过程**: [走了哪些弯路，最终怎么定位的]
- **解法**: [怎么修的]
- **状态**: 已解决 / 未解决(临时绕过: ...)
```

规则：
- **排查过程必记**——交接时最有价值的就是"怎么找到 bug 的"
- 未解决的坑标注临时绕过方案
- 已解决且无通用价值的坑可以在项目收尾时归档

**exploration-log.md** — 探索日志，持续追加的原始记录

```markdown
## YYYY-MM-DD HH:MM
- **尝试**: [做了什么]
- **结果**: [成功/失败/部分成功, 关键数字]
- **发现**: [学到了什么, 即使是负面的]
- **下一步**: [接下来打算做什么]
---
```

规则：
- **这是最宽松的文件**——允许记录失败的尝试、不确定的假设、临时的观察
- 每条记录 < 10 行，保持简洁
- 不做蒸馏——这个文件就是原始记录，蒸馏后的结论进 `.my_memory/research/`
- 容量上限 ~200 条，超过后按时间归档旧条目到 `exploration-log-archive.md`

### 8.4 项目记忆的使用流程

**新会话启动时**：
1. 检测当前工作目录是否属于某个已知项目（有 `.my_memory/` 或匹配项目列表）
2. 如果有 → 读取 `overview.md`，了解项目当前状态
3. 如果用户提到具体任务 → 额外读取 `decisions.md` 和 `pitfalls.md` 的相关条目
4. 如果是探索/调试类任务 → 额外读取 `exploration-log.md` 的最近 10 条

**工作过程中**：
- 做出决策时 → 实时写入 `decisions.md`（不要等到会话结束）
- 踩到坑时 → 实时写入 `pitfalls.md`
- 尝试新方案时 → 实时追加 `exploration-log.md`
- **关键**：不要攒到会话结束再写。对话中途就可能丢失上下文。

**会话结束时**：
1. 更新 `overview.md` 的"当前阶段"、"当前结论"、"未解决问题"
2. 将 exploration-log 中的重要发现提炼为 P2 条目写入 `.my_memory/research/` 对应文件（带溯源格式 §3.1.1）
3. 将新发现的坑提炼为 P1 条目写入 `.my_memory/conventions.md`（如果是跨项目通用的）

### 8.5 项目交接场景

当用户说"交接这个项目"或"总结一下项目进展"时：

1. 读取 `.my_memory/` 下所有文件
2. 生成结构化交接文档：
   - 项目目标与当前状态（from overview.md）
   - 关键决策链（from decisions.md，按时间线串联）
   - 已知坑与未解决问题（from pitfalls.md）
   - 探索历史摘要（from exploration-log.md，按阶段归纳）
   - 下一步建议
3. 交接文档输出为 markdown 文件，而非仅在对话中展示

---

## 9. 自检清单

### 写入前检查

- [ ] 这条信息未来会话还会用到吗？（F1）
- [ ] 现有记忆里有没有等价内容？（F2）
- [ ] 能从代码/配置推导出来吗？（F3）
- [ ] 该放 P0/P1/P2 还是只进日志？
- [ ] 写入后该 section 是否超预算？
- [ ] 和已有条目有没有矛盾？

### 用户纠正时额外检查（§1.4）

- [ ] 是否先检索了已有记忆？
- [ ] 如果已有 → 是否检查了格式/位置问题？
- [ ] 如果是第 N 次提醒 → 是否升级为 P0？

### 实验结论额外检查（§3.1.1）

- [ ] 是否附带了置信度？
- [ ] 证据链是否完整（数据来源 + 关键步骤 + 前提假设）？
- [ ] 完整推理过程是否写入了日志？

### 项目工作中额外检查（§8）

- [ ] 当前工作目录是否有 `.my_memory/`？
- [ ] 做出决策后是否实时写入了 `decisions.md`？
- [ ] 踩坑后是否实时写入了 `pitfalls.md`？
- [ ] 探索尝试是否追加到了 `exploration-log.md`？
